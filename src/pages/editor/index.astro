---
import { MarkdownResumeEditor } from "@/components/editor/MarkdownResumeEditor";
import BaseLayout from "@/layouts/BaseLayout.astro";
---

<BaseLayout
  title="Markdown Resume Editor"
  description="Write a resume in Markdown, preview it live, switch templates, import or export Markdown, and print to PDF in the browser."
  noIndex={true}
>
  <section class="editor-shell">
    <MarkdownResumeEditor client:only="react" />
  </section>
</BaseLayout>
